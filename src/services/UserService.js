const e = require("express")

const createUser = () => {
    return newPromise((resolve,reject) => {
     try {
        resolve({})
     } catch (error) {
        
     }   
    })
}
module.exports = {
    createUser
}